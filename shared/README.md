# shared — 논문 간 재사용 유틸

이 저장소 안의 여러 논문이 공유하는 헬퍼(예: 공통 도표 스타일, 표 포맷터).

주의: 코퍼스의 정규화 로직(normalize.py)은 이곳이 아니라 `xiaohua-corpus`가
정본을 소유한다. 동일 결과가 필요하면 코퍼스를 버전 고정해 import 하거나
해당 스크립트를 각 논문의 code/로 복사(vendoring)한다.
